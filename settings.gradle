pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "org.jetbrains.kotlin.android" version "1.9.22"
    }
}

rootProject.name = "EmptyApp"
include(":app")
