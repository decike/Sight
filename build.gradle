// 使用现代 dependencyResolutionManagement 而不是旧的 buildscript classpath
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
